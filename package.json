{
  "name": "ai-character-chat",
  "version": "1.0.0",
  "description": "SPA para chatear con personajes usando AI (Gemini + Vercel)",
  "type": "module",
  "scripts": {
    "build": "echo \"No build step required\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": "Emanuel Florez",
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.0.0"
  },
  "vitest": {
    "environment": "jsdom"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
